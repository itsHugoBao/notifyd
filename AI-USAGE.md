# AI 使用说明

本作业按题目要求全程使用 AI 协作完成。这份文档如实记录 AI 用在了哪里、
AI 的哪些建议被否决、以及哪些关键决策由人做出。

## 工作流

采用 SDD（Spec-Driven Development），AI 分工如下：

1. **需求分析与 spec 起草** — Claude（Claude Code CLI）通读题目后起草
   `specs/spec.md`，人工评审后定稿；
2. **交叉评审** — 用 Grok 4.5 做两轮独立评审，结论均由人逐条仲裁：
   - *spec 评审*：采纳 3 处契约缺口、否决 4 处（见下文）；
   - *代码 CR*：确认 1 个 Critical 缺陷——投递结果写库复用了已超时的
     attempt context，持续挂起的供应商会无限重投、永不消耗重试预算。
     连同 2 处防御缺口（迟到写回守卫、配置约束校验）一并修复，
     并补上挂起超时与 3xx 的回归测试；
3. **任务拆解与实现** — Claude 按 `specs/tasks.md` 逐任务实现，每个任务
   独立 commit，人工 review 代码与测试后提交；
4. **验证** — 单元 + 端到端测试（`-race`、`-count=2` 防 flaky），外加真实
   二进制的手工冒烟测试（提交 → 观察重试 → 优雅停机）。

commit 历史与该流程一一对应，可作为过程的旁证。

## AI 在哪些关键地方提供了帮助

- **把模糊需求收敛成契约**：spec 的状态机、失败分类表（§5.2）、幂等三分支
  （§3.1）都是 AI 起草后人工确认的——AI 在"穷举边界情况"上明显快于人；
- **全部代码初稿与测试设计**：约 600 行实现 + 700 行测试由 AI 生成，
  人工 review 每个 commit；
- **发现自己的坑**：AI 在冒烟测试中发现未传 headers 时响应为
  `"headers":null` 并修正；本机代理拦截 localhost 导致的 503 也由 AI 定位。

## AI 给出过、但未被采纳的建议

| 建议来源 | 建议内容 | 不采纳的原因 |
|---------|---------|-------------|
| 常规方案（Claude 备选） | 引入 Kafka / RabbitMQ / Redis 做队列 | 入队与 ack 需同一事务；重试调度在 SQL 里更简单；低流量下 broker 是纯运维成本。这是本设计最核心的"反教科书"取舍（README §取舍） |
| 常规方案 | 加供应商模板 / 格式适配层 | 恰是题目想看你**不做**的复杂度：适配是业务逻辑，进了本服务边界就永远稳定不下来 |
| 常规方案 | 引入 web 框架（gin/chi）与 uuid 库 | Go 1.22+ 标准库路由够用；uuid 手写 10 行。依赖最小化让整个服务只有一个第三方依赖 |
| Grok 评审 | 把「未采纳的 AI 建议」写进 spec | spec 保持产品/技术契约的单一职责，AI 叙事放本文档 |
| Grok 评审 | GET 响应的 JSON shape、outbound TLS 策略写进 spec | 实现细节，进代码与 README 即可，spec 不必过度具体 |
| Grok 评审 | （备选）砍掉幂等键或 redeliver API 以缩 scope | 幂等键防调用方重试放大重复，redeliver 是"长期不可用"必答题的可操作答案——两者都是可靠性叙事的组成部分，保留 |

Grok 评审中被**采纳**的部分（spec 轮：幂等冲突 409 语义、认领原子性、kill -9
兜底说明；代码 CR 轮：写库 context 缺陷、迟到写回守卫、配置校验与三处 minor），
同样经人工逐条核对代码确认成立后才修复——采纳与否决都留有记录（git log）。
值得一提：Critical 缺陷之所以漏网，是因为初版 e2e 的假供应商都是快速响应，
测不到「挂起耗尽超时」路径——交叉评审的价值正在于打破单一模型的盲区共振。

## 哪些关键决策由人做出，以及原因

| 决策 | 原因 |
|------|------|
| 技术栈选 Go | 并发模型贴合投递 worker + 重试调度的叙事，单二进制零依赖部署；也是候选人后续技术面能深入讨论的栈 |
| SQLite-as-queue，拒绝 MQ | 见上表第一行；AI 会给出两种方案，**选哪种、为什么选**是人的判断 |
| at-least-once 而非"尽量 exactly-once" | 诚实性：HTTP 上 exactly-once 是营销话术，承认重复并提供去重抓手才是真实工程 |
| 4xx 一次即死信 | 快速暴露配置错误优于对着 bug 消耗重试预算——这是运维经验，不是 AI 默认倾向（AI 初稿倾向对所有失败统一重试） |
| 幂等冲突返回 409 而非静默返回旧记录 | 同 key 不同 payload 是调用方 bug，报错让 bug 尽早浮出水面 |
| scope 控制 | 哪些进 v1、哪些进演进路径（spec §2.2 / §8）的每一条线，都是对照 4 小时时间盒画的 |

## 一句话总结

AI 负责生成与穷举，人负责边界与取舍——本仓库里所有"不做什么"的决定都是人做的。
